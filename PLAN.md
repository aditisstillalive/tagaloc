I want to build app that put watermark to an uploaded picute. This app will be deployed in vercel, make it compatible.
Initial page is where the user upload a picture, below the image uploader, there's a text box to insert the location. The web then get the geolocation (latituda longitude)
and put it in the picture as watermark on the bottom right. The watermarked picture then can be downloaded.
